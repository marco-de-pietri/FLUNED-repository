============
User's Guide
============

.. toctree::
   :maxdepth: 1
   :hidden:

  keywords

A generic template can be created by running `fluned -t`

If two or more set of input keywords are included in the input file, the FLUNED pre-processor will create a case for each keyword set.
The keywords set must be separated by at least one empty line and the CASE parameters cannot be repeated.
